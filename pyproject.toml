[project]
name = "heatops-ai"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-dotenv",
    "pydantic",
    "openai",
    "requests",
    "httpx"
]
[tool.setuptools]
packages = ["heatops_ai"]